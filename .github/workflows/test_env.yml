name: "Test env"

on:
  workflow_dispatch:

jobs:
  Windows:
    if: github.ref_name != 'zh-cn' && github.head_ref != 'zh-cn'
    runs-on: windows-latest
    steps:
    - name: List redist
      run: ls -R %VCToolsRedistDir%x64 
